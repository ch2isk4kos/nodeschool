/*
  We can do basic math using familiar operators like +, -, *, /, and %.
  For more complex math, we can use the Math object.

  Math.round() is a method that rounds either up or down to the nearest integer.
*/

let roundUp = 1.5;
let rounded = Math.round(roundUp);
console.log(rounded);
